# ============================================
# nianming_nav Cloudflare Workers 部署配置
# ============================================

name = "nianming_nav"
main = "worker/index.js"
compatibility_date = "2024-01-01"

# 静态资源目录 (Workers Sites)
[site]
bucket = "./dist"

# KV 命名空间绑定
# [[kv_namespaces]]
# binding = "NAV_KV"
# id = "your-kv-namespace-id"

# 环境变量 (也可以在 Cloudflare Dashboard 中设置)
# [vars]
# SYNC_PASSWORD = "your-password"
